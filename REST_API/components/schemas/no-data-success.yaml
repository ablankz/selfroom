NoDataSuccess:
  description: 空の成功データ
  type: object
  required: [success, data, code, message, validationMessages]
  properties:
    success: { type: boolean, example: true }
    data: { type: array, nullable: true, example: [] }
    code: { type: integer, example: 0 }
    message: { type: string, example: "OK" }
    validationMessages: { type: array, maximum: 0, example: [] }
